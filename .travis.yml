language: node_js
node_js:
  - '12.16.1'

before_install: cd packages/kvlt-konverter

jobs:
  include:
    - stage: npm release
      if: tag IS present
      node_js: '12.16.1'
      script: echo 'Deploying to npm ...'
      deploy:
        provider: npm
        api_key: $NPM_API_KEY
        on:
          branch: master
          tags: true
